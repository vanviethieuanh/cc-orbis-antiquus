[package]
name = "cc-orbis-antiquus"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = "0.18.1"
clap = { version = "4.6.0", features = ["derive"] }
earcut = "0.4.5"
geo = "0.32.0"
shapefile = "0.7.0"

[profile.dev]
opt-level = 1

[profile.release]
opt-level = 3
lto = true
